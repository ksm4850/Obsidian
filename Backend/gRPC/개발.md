목표 플로우
![](images/Pasted%20image%2020260106171702.png)

1. [사전] Worker가 gRPC 서버에 스트림 연결 (GetJobs)
         └── 연결 유지, 작업 대기 중

2. 브라우저 → FastAPI: POST /inference 요청

3. FastAPI → gRPC 서버: SubmitJob() 호출

4. gRPC 서버 → Worker: 스트림으로 Job 전달 (이미 연결됨)

5. Worker: GPU 연산 수행

6. Worker → gRPC 서버: SubmitResult() 결과 전송

7. gRPC 서버 → FastAPI: 결과 반환

8. FastAPI → 브라우저: HTTP 응답